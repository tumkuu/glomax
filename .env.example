# Session (required in production — Render can auto-generate)
SESSION_SECRET=change-me-to-a-long-random-string

# Gmail SMTP (local only — often broken on Render due to IPv6)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=tumkuu1223@gmail.com
SMTP_PASS=bvtcgknjxrcsxpbd
SMTP_FROM=GloMax Orders <tumkuu1223@gmail.com>

# Where order emails are delivered
# Resend free tier: MUST be the email you signed up with on resend.com
ORDER_EMAIL_TO=kh.tuguldur99@gmail.com

# Resend HTTPS (recommended for Render) — https://resend.com
RESEND_API_KEY=re_xxxxxxxx
RESEND_FROM=GloMax Orders <onboarding@resend.dev>
