# Session (required in production — Render can auto-generate)
SESSION_SECRET=change-me-to-a-long-random-string

# Gmail SMTP (App Password required — not your normal Gmail password)
# Create one at: https://myaccount.google.com/apppasswords

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tumkuu1223@gmail.com
SMTP_PASS=your-16-char-app-password
SMTP_FROM="GloMax Orders <tumkuu1223@gmail.com>"

# Where new order emails are delivered (must also set this on Render!)
ORDER_EMAIL_TO=glomex654@gmail.com
